# BaseAgent 미구현 기능 분석 보고서

현재 `BaseAgent` 프로젝트는 기본적인 LLM 연동, 에이전트 루프, 단기 메모리(RAM), 단순 동기식 도구 실행(Tool Calling) 위주로 구현된 경량 프레임워크입니다. 실제 프로덕션 수준의 서비스를 위해 아직 구현되지 않은 핵심 기능 목록은 다음과 같습니다.

---

## 1. 메모리 및 데이터 영속성 (Memory & Persistence)

* **메모리 압축 및 요약 (Memory Summarization)**
  * **설명**: 지정된 대화 한도(`max_messages`)를 초과하면 단순히 오래된 메시지부터 무조건 잘라내는(Truncation) 방식입니다.
  * **해결 방안**: 중요 정보를 잃지 않도록 과거 대화를 주기적으로 요약(Summarization)하여 하나의 압축 컨텍스트로 보존하는 로직이 요구됩니다.
* **사용자 프로필 기억 (User Profile Memory)**
  * **설명**: 사용자의 성향, 과거 이력, 개인 설정 정보를 별도 프로필 딕셔너리로 추출 및 관리하는 기능이 없습니다.

---

## 2. 도구(Tool) 실행 고도화

* **도구 검색기 (RAG-based Tool Selection)**
  * **설명**: 에이전트에 등록된 모든 도구 스키마(JSON Schema)를 매번 LLM API 전송 시 전송하므로, 도구가 늘어날수록 입력 토큰과 비용이 기하급수적으로 증가하며 AI 성능이 하락합니다.
  * **해결 방안**: 도구 설명글의 벡터 검색(Semantic Search)을 통해 사용자 쿼리에 어울리는 최적의 2~3개 도구만 LLM에 전달하는 RAG 기반 도구 선택 방식이 필요합니다.
* **비동기/병렬 도구 실행 (Concurrent Tool Execution)**
  * **설명**: LLM이 여러 개의 도구 실행(`tool_calls`)을 요청할 때 현재는 `for` 루프로 하나씩 순차 실행하므로 대기 시간이 길어집니다.
  * **해결 방안**: 비동기 멀티스레딩 또는 `asyncio.gather`를 통해 여러 도구를 동시에 실행하는 구조로 개선해야 합니다.
* **사용자 동의 절차 (Human-in-the-loop)**
  * **설명**: 파일 삭제, DB 수정 등 민감한 동작을 하는 도구를 실행하기 전에 사용자 동의를 얻는 안전장치가 부재합니다.

---

## 3. 다중 에이전트 및 오케스트레이션 (Multi-Agent & Orchestration)

* **다중 에이전트 조율 (Multi-Agent Orchestration)**
  * **설명**: 복잡한 비즈니스 프로세스(예: 기획자 에이전트가 문서를 쓰고, 코더 에이전트가 코딩을 하며, 테스터 에이전트가 테스트하는 등)를 조율하는 오케스트레이션 메커니즘이 없습니다.
  * **해결 방안**: 에이전트 간 메시지 전달 구조(Agent-to-Agent Messaging), 라우터 에이전트(Router Agent), 감독관 에이전트(Supervisor Agent) 아키텍처 도입이 필요합니다.

---

## 4. LLM 통신 및 파싱 제어

* **도구 결합형 스트리밍 (Hybrid Streaming)**
  * **설명**: 현재는 텍스트를 실시간으로 출력하는 스트리밍 모드(`stream`)에서는 도구(Tool) 호출을 지원하지 않습니다.
  * **해결 방안**: LLM의 스트리밍 출력을 실시간으로 조각조각 받으면서도 중간에 도구 호출 이벤트가 있으면 낚아채서 실행할 수 있는 통합 스트리밍 처리 로직이 필요합니다.
* **구조화된 출력 검증 (Structured Output Validation)**
  * **설명**: LLM이 반환하는 JSON 형태의 데이터 포맷을 보장하거나 검증하는 장치가 없어 AI가 엉뚱한 필드를 만들어 보내면 파싱 예외가 발생할 위험이 큽니다.
  * **해결 방안**: OpenAI의 `Structured Outputs` 혹은 Pydantic 스키마 정의를 사용한 강제 검증 인터페이스 결합이 요구됩니다.

---

## 5. 비용 및 보안 감시

* **토큰 예산 한도 관리 (Token Budgeting)**
  * **설명**: 무한 도구 루프에 빠지거나 악성 사용자 쿼리에 의해 대량의 요금이 과금되는 것을 막는 비용/토큰 한도 설정 기능이 부재합니다.
  * **해결 방안**: 호출 세션당 최대 허용 비용(달러 단위) 혹은 토큰 사용량 상한선을 추적하고 제한하는 기능이 필요합니다.
